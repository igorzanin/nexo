{
  "name": "nexo",
  "private": true,
  "workspaces": [
    "webapp",
    "desktop"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=webapp",
    "dev:desktop": "npm run dev --workspace=desktop",
    "electron:dev": "npm run electron:dev --workspace=desktop",
    "build:web": "npm run build --workspace=webapp",
    "build:desktop": "npm run build --workspace=desktop",
    "test:web": "npm run test --workspace=webapp",
    "lint": "npm run lint --workspace=webapp"
  }
}
